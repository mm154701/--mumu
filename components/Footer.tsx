import React from 'react';

export const Footer: React.FC = () => {
    return (
        <footer className="w-full max-w-3xl mt-8 text-center text-gray-500 text-sm">
            <p>由 React、TypeScript 和 Tailwind CSS 构建。所有处理都在您的浏览器中完成。</p>
        </footer>
    );
};